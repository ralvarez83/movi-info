# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sw?

# Artefactos de compilación de .NET. Un patrón sin barra inicial se aplica a cualquier nivel,
# así que cubre todos los proyectos de la solución sin tener que listarlos uno a uno.
bin/
obj/

# Secretos locales: el token de TheMovieDB se inyecta por variable de entorno o por
# "dotnet user-secrets", nunca en un fichero versionado.
appsettings.*.local.json
.env.local
.env.*.local
