# En producción el front no conoce la URL del back: llama a una ruta relativa y es el proxy del
# hosting (ver netlify.toml) quien la reenvía. Así el navegador nunca ve el dominio de Render y
# no hay peticiones cross-origin.
VITE_DOT_NET_BACK=/api/
