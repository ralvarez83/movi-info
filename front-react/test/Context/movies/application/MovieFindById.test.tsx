import {expect, test} from '@jest/globals';

describe('If i search a movie by a ..', () => {
  test('existing ID then I receibe info about the movie', () => {
    
  
    //expect(() => binaryStringToNumber('abc')).toThrow(CustomError);
  });
  
});
